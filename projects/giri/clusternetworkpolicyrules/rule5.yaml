apiVersion: security.k8smgmt.io/v3
kind: ClusterNetworkPolicyRule
metadata:
  name: rule5
  project: giri
spec:
  artifact:
    artifact:
      paths:
      - name: file://artifacts/rule5/cluster-wide.yaml
    options: {}
    type: Yaml
  version: v5
