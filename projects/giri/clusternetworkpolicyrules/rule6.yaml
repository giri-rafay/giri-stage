apiVersion: security.k8smgmt.io/v3
kind: ClusterNetworkPolicyRule
metadata:
  name: rule6
  project: giri
spec:
  artifact:
    artifact:
      paths:
      - name: file://artifacts/rule6/cluster-wide.yaml
    options: {}
    type: Yaml
  version: v6
